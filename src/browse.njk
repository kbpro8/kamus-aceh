---
layout: base.njk
title: Daftar A–Z
description: Telusuri semua entri kamus Aceh secara alfabetis.
permalink: /browse/
---
<h1 class="page-title">Daftar A–Z</h1>

{% set byLetter = entries | byLetter %}

<nav class="az-nav" aria-label="Navigasi huruf">
  {% for letter, items in byLetter %}
  <a href="#letter-{{ letter }}">{{ letter }}</a>
  {% endfor %}
</nav>

{% for letter, items in byLetter %}
<section class="letter-group" id="letter-{{ letter }}">
  <h2 class="letter-heading">{{ letter }}</h2>
  <ul class="entry-list">
    {% for entry in items %}
    <li>
      <a href="{{ ('/entries/' + entry.fileSlug + '/') | url }}">{{ entry.headword }}</a>
      <span class="gloss"> — {{ entry.definitions[0].meaning_id }}</span>
    </li>
    {% endfor %}
  </ul>
</section>
{% endfor %}
