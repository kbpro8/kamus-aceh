{
  "name": "kamus-aceh",
  "version": "1.0.0",
  "description": "Acehnese-Indonesian-English dictionary — static site built with Eleventy",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy && npx pagefind --site _site",
    "clean": "rm -rf _site"
  },
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pagefind": "^1.3.0"
  },
  "dependencies": {
    "js-yaml": "^5.2.1"
  }
}
